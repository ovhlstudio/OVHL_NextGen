{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "🧩 Start Rojo Server",
            "type": "shell",
            "command": "rojo serve",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "close": false
            }
        },
        {
            "label": "🧠 Auto Commit & Sync",
            "type": "shell",
            "command": "git add . && git commit -m \"auto: sync from VSCode\" && git push",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "none",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        },
        {
            "label": "📘 AI Documentation Commit",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-Command",
                "$changed = git diff --name-only HEAD | Where-Object { $_ -like 'Docs/*.md' }; if ($changed) { $log = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'; Add-Content Docs/10_CHANGELOG.md \"`n### [$log] - Auto AI Commit`nUpdated Docs:`n$($changed -join \"`n\")`n[Generated by: GPT-5_OVHL | Mode: AutoCommit]`n\"; git add .; git commit -m 'auto: docs update via AI'; git push } else { Write-Host 'No Docs changes detected.' }"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "group": "none",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}